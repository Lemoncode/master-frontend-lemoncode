import {StorageService} from "./storage-service.interface";

export class ApiBackendService implements StorageService
{
    find(productID) {
        // ...
    }

    add(product) {}
    update(product) {}
    remove(product) {}
}
