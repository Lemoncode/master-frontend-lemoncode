import React from "react";

import { Terminal, TerminalProps } from "./pods/terminal.component";

export const MicroappTerminal: React.FC<TerminalProps> = (props) => <Terminal {...props} />;

export default MicroappTerminal;
