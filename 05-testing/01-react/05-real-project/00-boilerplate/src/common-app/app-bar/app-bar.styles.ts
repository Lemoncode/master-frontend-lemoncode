import { css } from 'emotion';
import { theme } from 'core/theme';

export const menuButton = css`
  margin-right: ${theme.spacing(0.1)}rem;
`;

export const loginText = css`
  margin-left: auto;
  padding-right: ${theme.spacing(0.1)}rem;
`;
