{
  "name": "02-xss-exercise",
  "version": "1.0.0",
  "description": "- Log into the application using as user _admin_ and password _admin_.\r - Once logged in we have received our token for the headers. And now let's see how to subtract them.\r - We insert this anchor in the textearea and send the request to the server.",
  "main": "index.js",
  "scripts": {
    "postinstall": "run-p -l backend:install frontend:install",
    "backend:install": "cd backend && npm install",
    "frontend:install": "cd frontend && npm install",
    "start": "run-p -l backend:start frontend:start",
    "backend:start": "cd backend && npm start",
    "frontend:start": "cd frontend && npm start"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
