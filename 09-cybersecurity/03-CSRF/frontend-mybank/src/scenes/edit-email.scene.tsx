import React from "react";
import { AppLayout } from "layouts";
import { EditEmailContainer } from "pods/edit-email";

export const EditEmailScene = () => (
  <AppLayout>
    <EditEmailContainer />
  </AppLayout>
);
